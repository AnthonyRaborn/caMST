% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_selection_function.R
\name{module_selection}
\alias{module_selection}
\title{Select Two MST Modules After a Fixed Routing Stage}
\usage{
module_selection(
  i,
  module_item_bank,
  modules,
  transition_matrix,
  theta_est,
  method = "BM",
  response_matrix,
  seen_cat_items,
  cat_length,
  module_select,
  initial_theta = 0
)
}
\arguments{
\item{i}{The row index of \code{response_matrix} for the person being
tested.}

\item{module_item_bank}{A data frame of MST item parameters, in \pkg{catR}
item-bank format, for the items referenced by \code{modules}.}

\item{modules}{A matrix describing the relationship between items and
modules; see \code{\link{multistage_test}}.}

\item{transition_matrix}{A matrix describing allowed transitions between
modules; see \code{\link{multistage_test}}.}

\item{theta_est}{The theta estimate (from the CAT routing stage) used to
select the first MST module.}

\item{method}{The provisional theta estimation method passed to
\code{catR::thetaEst}.}

\item{response_matrix}{A matrix or data frame of person responses.}

\item{seen_cat_items}{Names of the items already administered during the
CAT routing stage.}

\item{cat_length}{The number of items administered in the CAT routing
stage.}

\item{module_select}{The module-selection criterion passed to
\code{nextModule} as \code{criterion} (e.g. \code{"MFI"}); invalid or
\code{NULL} values fall back to \code{"MFI"}.}

\item{initial_theta}{The initial theta value passed to
\code{\link{iterative.theta.estimate}}.}
}
\value{
A list with \code{final.theta.estimate.mstR}, \code{eap.theta},
  \code{final.theta.iterative}, \code{sem.iterative}, \code{final.item.bank},
  \code{final.items.seen}, \code{modules.seen}, and \code{final.responses}
  for the one person tested.
}
\description{
Selects the next two modules after a CAT routing stage of exactly
\code{cat_length} items, hard-coded to a 3-stage (routing + 2 MST modules)
design. Unlike \code{\link{moduleSelectionCAMST}}, this does not loop over
an arbitrary number of stages and cannot be reused for other designs.
}
\details{
\strong{Note:} this function does not currently appear to be called
anywhere in the package; \code{\link{mixed_adaptive_test}} uses
\code{\link{moduleSelectionCAMST}} instead.
}
\keyword{internal}
