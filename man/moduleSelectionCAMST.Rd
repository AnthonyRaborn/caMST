% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_selection_function.R
\name{moduleSelectionCAMST}
\alias{moduleSelectionCAMST}
\title{Select MST Modules for an Arbitrary Number of Stages}
\usage{
moduleSelectionCAMST(
  i,
  module_item_bank,
  modules,
  transition_matrix,
  theta_est,
  method = "BM",
  response_matrix,
  seen_cat_items,
  cat_length,
  n_stage,
  module_select = NULL,
  final_theta_method = NULL
)
}
\arguments{
\item{i}{The row index of \code{response_matrix} for the person being
tested.}

\item{module_item_bank}{A data frame of MST item parameters, in \pkg{catR}
item-bank format, for the items referenced by \code{modules}.}

\item{modules}{A matrix describing the relationship between items and
modules; see \code{\link{multistage_test}}.}

\item{transition_matrix}{A matrix describing allowed transitions between
modules; see \code{\link{multistage_test}}.}

\item{theta_est}{The theta estimate (from the CAT routing stage) used to
select the first MST module.}

\item{method}{The provisional theta estimation method passed to
\code{catR::thetaEst}.}

\item{response_matrix}{A matrix or data frame of person responses.}

\item{seen_cat_items}{Names of the items already administered during the
CAT routing stage.}

\item{cat_length}{The number of items administered in the CAT routing
stage.}

\item{n_stage}{The total number of stages in the test, including the CAT
routing stage (so \code{n_stage - 1} MST modules are selected).}

\item{module_select}{The module-selection criterion passed to
\code{nextModule} as \code{criterion} (e.g. \code{"MFI"}); invalid or
\code{NULL} values fall back to \code{"MFI"}.}

\item{final_theta_method}{A character value indicating the method used
for the single final theta estimate returned. One of "BM", "ML", "WL",
"ROB" (passed to \code{catR::thetaEst}) or "EAP" (uses
\code{catR::eapEst}).}
}
\value{
A list with \code{final.theta.estimate.mstR}, \code{final.theta.SEM},
  \code{final.item.bank}, \code{final.items.seen}, \code{modules.seen}, and
  \code{final.responses} for the one person tested.
}
\description{
Selects the sequence of MST modules following a CAT routing stage, looping
over \code{n_stage - 1} module-selection steps. This is the function
\code{\link{mixed_adaptive_test}} actually uses to drive its MST portion.
}
\keyword{internal}
